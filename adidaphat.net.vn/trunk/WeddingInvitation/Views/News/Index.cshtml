@model WeddingInvitation.Core.Models.ContentManagement.NewsItem
@{
    ViewBag.Title = Model.Title;
}

<div class="item-page">
    <span style="color:#8b8b8b;font-size:24px;border-bottom:1px solid #CCC;line-height:32px;">@Model.Title</span>
    <br />
    <br>
    <div class="wrapper faq">
        @Html.Raw(Model.Full)
    </div>
</div>