@model WeddingInvitation.Models.ListTracingModel
@{
    ViewBag.Title = "Định vị bưu phẩm";
}
<div class="item-page">
    <span style="color:#8b8b8b;font-size:24px;border-bottom:1px solid #CCC;line-height:32px;">Định Vị Bưu Phẩm</span>
    <br />
    <br>
    @using (Html.BeginForm("Index", "Tracking", FormMethod.Get))
    {
        <div style="float:left;padding:3px;font-weight: bold;"><label style="color: #CC6666;">Mã BP/BK :</label></div>
        <div style="float:left;padding:3px;">@Html.TextAreaFor(model => model.TrackingNumbers, new { @style = "height:60px;width:400px;", @required = "required", @placeholder="Quý khách nhập số vận đơn trên phiếu gửi để tra bưu phẩm, bưu kiện" })</div>
        <div style="float:left;padding-top: 15px; padding-left: 10px;"><button class="button validate" type="submit">Tra cứu</button></div>    
    }
    <br/>
    <br/>
    <div style="clear: both"></div>
    <div style="line-height:5px;padding-top: 20px;">
        <p>*Theo dõi lên đến 5 số vận đơn trong một lần kiểm tra, cách nhau bằng một dấu phẩy (,)</p>
        <p>Ví dụ: Số vận đơn là AA150462, AA156257</p>
    </div>
    <div class="wrapper faq" style="padding-top:20px">
    @if (Model.Trackings.Count > 0)
    {
        foreach (var item in Model.Trackings)
        {
            Html.RenderPartial("_TracingDetail", item);
        }
    }
    else if (!string.IsNullOrEmpty(Model.TrackingNumbers))
    {
        <span style="color:#8b8b8b;padding:3px;">Không tìm thấy kết quả, vui lòng kiểm tra lại số vận đơn.</span>
    }
    </div>
</div>
